Программа позволяет получать информацию о вакансиях с платформы hh.ru в России, сохранять ее в файл и позволять удобно
работать с ней: сортировать вакансии по ЗП, выводить нужное количество вакансий, совершать выборку согласно запросу по
списку полученного количества вакансий.

Действия пользователя:
1. Ввести запрос на получение информации с платформы hh.ru
2. Ввести желаемое количество выводимых вакансий
3. Ввести ключевые слова через пробел для поиска вакансий по навыкам

В результате работы:
1. Получаем запрос с ресурса по заданному параметру
2. Загружаем полученные данные в JSON файл
3. Выгружаем из файла данные в список
4. Проводим вадализацию данных и создаем новый список с необходимыми для создания объектов класса полями
5. Сортируем полученный список по ЗП
6. Обрезаем полученный список по заданному пользователем параметру
7. Из списка формируем список объектов класса
8. Выводим список вакансий в терминал
9. Получаем параметр по поиску вакансий по заданному пользователем параметру поля навыков
10. Выводим полученный список на экран
11. По завершению программы очищаем JSON файл, сформированный на шаге 2

В коде реализованы классы:
- HHIntegration(наследник абстрактного класса APIIntegration) - используется для проверки подключения к hh.ru по API
- HH(наследник абстрактного класса Parser) - используется для получения данных по запросу пользователя с ресурса hh.ru
- JsonLoadRequests(наследник абстрактного класса LoadRequests) - используется для записи в JSON файл данных, полученных
из запроса очистки JSON файла по завершению программы
- ListVacancyObject(наследник абстрактного класса ListClassObject) - используется для формирования списка из JSON файла
для обработки
- ListToValidateList(наследник абстрактного класса ListModification) - используется для преобразования списка в список
с необходимыми для создания класса Vacancy валидированными данными
- SortList(наследник абстрактного класса ListModification) - используется для сортировки списка с валидированными данными
для создания объектов класса Vacancy
- CropList(наследник абстрактного класса ListModification) - используется для обрезки отсортированного списка с
валидированными данными для создания объектов класса Vacancy
- ListVacancyObject(наследник абстрактного класса ListClassObject) - используется для работы со списком объектов класса
Vacancy: добавление в список объекта класса, создание списка объектов класса, выборка объектов класса по параметру,
сортирока списка
- Vacancy - используется для создания и обратки вакансий: строковое представление объекта класса, методы сравнения(=, !=,
<=, <, >, >=), метод по созданию нового экземпляра класса, деструктор объекта класса.

Классы и методы покрыты тестами опционально.